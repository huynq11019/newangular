import { SumPriceorderPipe } from './sum-priceorder.pipe';

describe('SumPriceorderPipe', () => {
  it('create an instance', () => {
    const pipe = new SumPriceorderPipe();
    expect(pipe).toBeTruthy();
  });
});
